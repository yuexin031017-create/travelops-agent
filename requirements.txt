# 基础（milvus_lite 依赖 pkg_resources，需 setuptools<82）
setuptools>=69.0.0,<82

# 多智能体框架
agentscope==1.0.16
# AgentScope 1.0.16 imports this 1.x client API; newer MCP 2.x is incompatible.
mcp==1.30.0

# LLM 调用
requests==2.32.3

# RAG 与向量库（pymilvus[milvus_lite] 用于本地 .db 连接）
pymilvus[milvus_lite]==2.6.9
# The project uses a file-based Milvus Lite URI; 3.x treats that path as a directory.
milvus-lite==2.5.1
sentence-transformers==5.2.3

# CLI 界面
rich==13.9.4

# 网络搜索（DDGS）
ddgs==9.10.0
